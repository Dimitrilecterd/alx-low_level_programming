#include "main.h"
/**
 * main - a function that prints the alphabet, in lowercase, followed
 * Return: 0
 */
void print_alphabet(void)
{
	char x;
		for (int x = 'a'; x <= 'z'; x++)
		{
			_putchar(x)
		}
		_putchar('\n');
	return (0);
}
